{
  "key": "merged_fleet_options_v1",
  "displayName": "Merged Fleet Options",
  "description": "Distinct fleet options from merged well index.",
  "sql": "SELECT * FROM (SELECT DISTINCT COALESCE(NULLIF(fleet_name, ''), 'Unknown') AS label, COALESCE(NULLIF(fleet_name, ''), 'Unknown') AS value FROM featurestore:merged_well_index_v1 ORDER BY label) q",
  "valueSchema": {
    "type": "object",
    "properties": {
      "label": {
        "type": "string"
      },
      "value": {
        "type": "string"
      }
    },
    "required": []
  },
  "paramsSchema": {
    "type": "object",
    "properties": {},
    "required": []
  }
}
