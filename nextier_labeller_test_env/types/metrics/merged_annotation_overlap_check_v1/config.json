{
  "key": "merged_annotation_overlap_check_v1",
  "displayName": "Merged Annotation Overlap Check",
  "description": "Merged dashboard copy of annotation_overlap_check_v1.",
  "sql": "SELECT * FROM ( SELECT 0 AS overlap_count WHERE :layer_id IS NOT NULL AND :annotation_id IS NOT NULL AND :start_time IS NOT NULL AND :end_time IS NOT NULL ) q",
  "valueSchema": {
    "type": "object",
    "properties": {
      "overlap_count": {
        "type": "number"
      }
    },
    "required": []
  },
  "paramsSchema": {
    "type": "object",
    "properties": {
      "layer_id": {
        "type": "string"
      },
      "annotation_id": {
        "type": "string"
      },
      "start_time": {
        "type": "string",
        "format": "date-time"
      },
      "end_time": {
        "type": "string",
        "format": "date-time"
      }
    },
    "required": [
      "layer_id",
      "annotation_id",
      "start_time",
      "end_time"
    ]
  }
}