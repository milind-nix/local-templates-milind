{
  "key": "copper_stage_options_v1",
  "displayName": "Copper Stage Options",
  "description": "Copper stage dropdown options for a well, from merged_copper_stage_index_v1.",
  "sql": "SELECT * FROM (SELECT copper_stage_num AS label, copper_stage_num AS value, stage_start_ts, stage_end_ts, sample_count FROM featurestore:merged_copper_stage_index_v1 WHERE name = :well_name ORDER BY copper_stage_num) q",
  "valueSchema": {
    "type": "object",
    "properties": {
      "label": { "type": "number" },
      "value": { "type": "number" },
      "stage_start_ts": { "type": "string", "format": "date-time" },
      "stage_end_ts": { "type": "string", "format": "date-time" },
      "sample_count": { "type": "number" }
    },
    "required": []
  },
  "paramsSchema": {
    "type": "object",
    "properties": {
      "well_name": { "type": "string" }
    },
    "required": ["well_name"]
  }
}
