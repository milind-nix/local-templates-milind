{
  "key": "live_family_options_v1",
  "displayName": "Live Family Options",
  "description": "Distinct well family options from live well index.",
  "sql": "SELECT * FROM (SELECT DISTINCT well_family AS label, well_family AS value FROM featurestore:live_well_index_v1 WHERE well_family IS NOT NULL ORDER BY well_family) q",
  "valueSchema": {
    "type": "object",
    "properties": {
      "label": {
        "type": "string"
      },
      "value": {
        "type": "string"
      }
    },
    "required": []
  },
  "paramsSchema": {
    "type": "object",
    "properties": {},
    "required": []
  }
}
